#include <iostream>
#include <string>

using namespace std;

int main() {
    int n,p;
    cin >> n >> p;
    while(n--) {
        string temp;
        cin >> temp;
    }
    cout << p;
    return 0;
}