#include <iostream>
#include <algorithm>
#include <vector>

using namespace std;

int main() {
    int n, h;
    cin >> n >> h;
    vector<int>a;
    while(n--) {
        int temp;
        cin >> temp;
        a.push_back(temp);
    }
    
    return 0;
}