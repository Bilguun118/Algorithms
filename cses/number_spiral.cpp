#include <iostream>

using namespace std;

#define ll long long

int main() {
    int n;
    int ans = 0;
    cin >> n;
    while(n--) {
        ll x, y;
        cin >> x >> y;
        // find out spiral and print
    }
    return 0;
}