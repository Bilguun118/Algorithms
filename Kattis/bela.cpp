#include <iostream>
#include <vector>
#include <string>

using namespace std;


int main() {
    int N;
    cin >> N;
    N *= 4;
    while(N--) {
        string cards;
        cin >> cards;
    }
    return 0;
}