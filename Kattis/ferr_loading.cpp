#include <iostream>
#include <string>
#include <vector>

using namespace std;


void ferry_loading() {
    
    return;
}

int main() {
    int TC;
    cin >> TC;
    while(TC--) {
        int L,M;
        cin >> L >> M;
        while(M--) {
            int temp;
            string direction;
            cin >> temp >> direction;
        }
    }
    return 0;
}