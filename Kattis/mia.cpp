#include <iostream>

using namespace std;

int main() {
    int d1,d2,d3,d4;
    while(1) {
        scanf("%d %d %d %d", &d1,&d2,&d3,&d4);
        if(d1 == 0 && d2 == 0 && d3 == 0 && d4 == 0) {
            break;
        }
        // mia roll dice
    }
    return 0;
}   